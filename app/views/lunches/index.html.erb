<table>
	<a href="/lunches/new">Add New</a>
	<tr>
		<th>Picture</th>
		<th>Repeatable</th>
		<th>Spiciness</th>
	</tr>
	<% if @lunches %>
		<% @lunches.each do |lunch| %>
			<tr>
				<td>
					<a href src="<%= lunch.photo_url %>"><%= lunch.owner %>'s lunch</a>
				</td>
				<td><%= lunch.repeatable %></td>
				<td><%= lunch.spiciness %></td>
				<td><a href="lunches/<%= lunch.id %>/edit">Edit</a></td>
			</tr>
		<% end %>
	<% end %>
</table>
